// 両替
// hard
// 異なる額面の硬貨を表す整数によって構成される配列 coins と、金額を表す整数 amount が与えられるので、金額 amount を構成するのに必要な最も少ない枚数の硬貨を返す minCoinChange という関数を作成してください。もし、その金額が硬貨のどの組み合わせでも補えない場合は、-1 を返してください。

// 例えば、配列 [1,3,4,5] と金額 7 が与えられた場合、1 + 1 + 5 = 7 の計 3 枚が考えられますが、最適解として 3 + 4 = 7 の計 2 枚を返します。

// 関数の入出力例

// 入力のデータ型： integer[] coins, integer amount

// 出力のデータ型： integer

// minCoinChange([1],5) --> 5

// minCoinChange([5],8) --> -1

// minCoinChange([2,3,8,7],12) --> 3

// minCoinChange([1,3,4,5],7) --> 2

function minCoinChange(coins:number[], amount:number) {
  // 関数を完成させてください
}